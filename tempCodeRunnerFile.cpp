 // while (fscanf(file, "%s %s %s %s %d %d", temp.tanggal, temp.kodeProduk, temp.namaProduk, temp.kategoriProduk, &temp.stock, &temp.hargaPerStock) != EOF)
    // {
    //     insertNodeBack(temp.tanggal, temp.kodeProduk, temp.namaProduk, temp.kategoriProduk, temp.stock, temp.hargaPerStock);
    // }